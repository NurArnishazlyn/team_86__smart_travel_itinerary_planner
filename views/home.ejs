<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>

    <!-- Header Section -->
    <header>
        <div class="logo">
            <img src="/images/logo.png" alt="Logo" class="logo">
        </div>
    </header>

    <!-- Welcome Section -->
    <h1>Welcome <%= user ? user.username : "Traveler" %>!</h1>

    <% if (user) { %>
        <a href="/logout">Logout</a>
    <% } else { %>
        <a href="/login">Login</a>
        <a href="/register">Register</a>
    <% } %>

    <!-- Footer Section -->
    <footer>
        <p><%= footerMessage %> - <%= currentYear %></p>
    </footer>

</body>
</html>
