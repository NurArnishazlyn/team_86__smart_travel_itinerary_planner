<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Guide</title>
    <link rel="stylesheet" href="/css/main.css">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    
  <title>Create New Blog Post</title>
  <style>
    body {
      font-family: sans-serif;
    }
    form {
      width: 500px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    label {
      display: block;
      margin-bottom: 5px;
    }
    input[type="text"],
    textarea,
    input[type="file"] { /* Added styling for file input */
      width: 100%;
      padding: 8px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 3px;
      box-sizing: border-box; /* Include padding and border in the element's total width and height */
    }
    textarea {
      height: 200px;
      resize: vertical; /* Allow vertical resizing of the textarea */
    }
    button[type="submit"] {
      background-color: #4CAF50;
      color: white;
      padding: 10px 15px;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    button[type="submit"]:hover {
      background-color: #45a049;
    }
    .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            background-color: pink;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .logout-btn {
            background-color: palevioletred;
            border: none;
            color: white;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
        }

        .logout-btn:hover {
            background-color: rgb(228, 36, 100);
            transition: 0.3s;
        }
        .logo{
            width: 100px;
            height: 100px;
            border-radius: 50%;
        }
  </style>
</head>
<body>

     <!-- Header Section -->
     <div class="header">
        <a href="/">
            <img src="/images/logo.png" alt="Logo" class="logo">
        </a>
        <div>
            <% if (user) { %>
                <span class="me-2">Welcome, <%= user.username %>!</span>
                <a href="/logout" class="logout-btn">Logout</a>
            <% } else { %>
                <a href="/login" class="logout-btn">Log In / Sign Up</a>
            <% } %>
        </div>
    </div>

  <h2 style="text-align: center;">Create New Blog Post</h2>

 
  



  <form action="/travel-guide/submit-blog-post" method="POST" enctype="multipart/form-data">
    <!--  action: updated according to previous file -->
    <!-- `enctype="multipart/form-data"` is VERY important for file uploads! -->

    <div>
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" required>
    </div>

    <div>
      <label for="country">Country:</label>
      <input type="text" id="country" name="country" required>
    </div>
    
    <div>
      <label for="content">Content:</label>
      <textarea id="content" name="content" required></textarea>
    </div>

    <div>
      <label for="author">Author:</label>
      <input type="text" id="author" name="author" value="<%= user.username %>" readonly>
      <!-- Populating with actual username -->
    </div>
     
    
    <div>
      <label for="image">Image:</label>
      <input type="file" id="image" name="image" accept="image/*">
    </div>

    <button type="submit">Publish</button>
  </form>

</body>
</html>
